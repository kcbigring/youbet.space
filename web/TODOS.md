Frontend next steps

- Complete flows:
  - Group creation
  - Wager create + deploy
  - Join wager (sign/relay)
  - Resolve/attest flow UI

- Wire SMS invite flow: call `POST /invite` then `/verify` to mark user verified.
- Add ephemeral wallet UX: create session on verify and store ephemeral auth token.

