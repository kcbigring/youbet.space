{
  "name": "friendly-wager-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "ethers": "^5.7.2",
    "hardhat": "^2.15.0"
  }
  ,
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:base": "npx hardhat run scripts/deploy.js --network base_testnet",
    "deploy:robinhood": "npx hardhat run scripts/deploy.js --network robinhood_testnet",
    "test": "npx hardhat test"
  }
}
