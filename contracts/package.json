{
  "name": "friendly-wager-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:base": "hardhat run scripts/deploy.js --network base_sepolia",
    "deploy:robinhood": "hardhat run scripts/deploy.js --network robinhood_testnet",
    "export-abi": "node scripts/export-abi.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^17.4.2",
    "ethers": "^6.13.0",
    "hardhat": "^2.22.0"
  }
}
